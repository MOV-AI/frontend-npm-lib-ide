import { makeStyles } from "@material-ui/styles";

export const detailsMenuStyles = makeStyles(_theme => ({
  detailsName: {
    margin: 0,
    padding: "16px",
    wordBreak: "break-all",
    textAlign: "center",
  }
}));
